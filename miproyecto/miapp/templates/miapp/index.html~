{% load staticfiles %}
<html>
<header>MI TIENDA DE VIDEOJUEGOS</header>
<head>
<title>videojuegos</title>
<link rel="stylesheet" type="text/css" href="{% static "css/videojuego.css" %}">
</head>
<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
			{% endif %}
<nav>
	<ul>
		{% if user.is_authenticated %}	
		<li><a href="{% url 'logout' %}">Logout</a></li>
			{% else %}
	 			<li><a href="{% url 'registro' %}">Registro</a></li>
				<li><a href="{% url 'login' %}">Login</a></li>
			{% endif %}
	</ul>
</nav>
{% if lista_videojuegos %}
	<table id="tabla" border="2">
		<tr>
			<td>Nombre</td>
			<td>fecha</td>
		</tr>
{% for videojuego in lista_videojuegos %}
		<tr>
			<td><a href="{% url 'detalle_videojuego' videojuego.id %}">{{ videojuego.nombre }}</a></td>
			<td>{{ videojuego.fecha_distribucion }}</td>
		</tr>
{% endfor %}
	</table>
		<div id="descripcion">
			
			<p>Esta es la web de compra y venta de videojuegos refetrente en cualquier ambito de las plataformas en que se desarrollan los videojuegos.<br> 
Las compras se haran con tarjeta o en su defecto a contrareembolso para mayor comodidad de los usuarios.<br>
Las hojas de reclamaciones estan a disposicion de los usuarios<br>
Estamos atentos a los comentariso por si hay algun incidente en nuestra pagina web.

</p>

		</div>
{% else %}
		<p>No hay videojuegos</p>			
{% endif %}
 
</body>
</html>
